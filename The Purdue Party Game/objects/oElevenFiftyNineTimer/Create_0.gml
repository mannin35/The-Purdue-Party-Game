/// @description Store timer
// You can write your code in this editor

//_timer = 0;
t_hour = 11
t_min = 59
t_sec = 45
t_msec = 0 // 1/60 of a frame


alarm[0] = 1

/*_timer += delta_time;

// Convert milliseconds to hours, minutes, seconds, and milliseconds
_hours = floor(_timer / (1000 * 60 * 60));
_minutes = floor((_timer mod (1000 * 60 * 60)) / (1000 * 60));
_seconds = floor((_timer mod (1000 * 60)) / 1000);
_milliseconds = _timer mod 1000;
*/
// Display the timer
//show_debug_message(string(t_hour) + ":" + string(t_min) + ":" + string(t_sec) + ":" + string(t_msec));







